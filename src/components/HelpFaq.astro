---
const faqs = [
  { question: "Как оформить заказ?", answer: "Выберите товар, добавьте его в корзину и оформите заказ через форму на сайте." },
  { question: "Как быстро доставляете?", answer: "Срок доставки зависит от региона и выбранного способа доставки." },
  { question: "Можно ли вернуть товар?", answer: "Да, возврат доступен по правилам магазина." },
  { question: "Есть ли поддержка?", answer: "Да, команда поддержки готова помочь с вопросами по заказу." },
  { question: "Как узнать статус заказа?", answer: "Статус заказа можно уточнить через контакты магазина." }
];
---
<section class="themesFlat">
  <div class="container">
    <div class="row">
      <div class="col-lg-6">
        <div class="sect-heading type-3 wow fadeInUp">
          <h1 class="s-title font-red_hat fw-semibold letter-space-0">
            Есть вопрос? <br />
            Мы рядом и готовы помочь.
          </h1>
          <p class="s-desc text-body-1 cl-text-2">
            Посмотрите ответы на частые вопросы покупателей. <br />
            Остались вопросы?
          </p>
          <a class="tf-btn animate-btn" href="/learn-more/">Купить сейчас</a>
        </div>
      </div>
      <div class="col-lg-6">
        <div class="d-grid gap-16" id="faq-accordion">
          {faqs.map((item, index) => {
            const id = `faq-${index + 1}`;
            const isOpen = index === 0;

            return (
              <div class="faq-accordion_item">
                <div
                  aria-controls={id}
                  aria-expanded={isOpen ? "true" : "false"}
                  class={`accordion-action h5 text-capitalize letter-space-05 ${isOpen ? "" : "collapsed"}`}
                  data-bs-target={`#${id}`}
                  data-bs-toggle="collapse"
                  role="button"
                >
                  <span>{item.question}</span>
                  <span class="icon ic-accordion-custom cl-2"></span>
                </div>
                <div class={`collapse ${isOpen ? "show" : ""}`} data-bs-parent="#faq-accordion" id={id}>
                  <p class="faq-content font-plus_jakarta text-body-1 cl-text-2">{item.answer}</p>
                </div>
              </div>
            );
          })}
        </div>
      </div>
    </div>
  </div>
</section>
